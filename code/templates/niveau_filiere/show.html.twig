{% extends 'BaseInscriptionConnexion.html.twig' %}

{% block title %}NiveauFiliere{% endblock %}

{% block body %}
    <h1>NiveauFiliere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau_filiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ niveau_filiere.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('niveau_filiere_index') }}">back to list</a>

    <a href="{{ path('niveau_filiere_edit', {'id': niveau_filiere.id}) }}">edit</a>

    {{ include('niveau_filiere/_delete_form.html.twig') }}
{% endblock %}
